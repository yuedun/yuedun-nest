<div class="section-nine">
    <div class="cooperation-box width-visual">
        <h2 class="common_title">{{item.title_1}}<i></i></h2>
        <ul class="clearfix">
        {% for item2 in item.elements %}
            <li class="col_3"><img src="{{item2.img}}" alt="{{item2.title_1}}"></li>
        {% endfor %}
        </ul>
    </div>
</div>